# Normalize line endings: store text as LF in the repo, check out native on Windows
* text=auto eol=lf

# Treat binary assets as binary (no EOL conversion, no diff noise)
*.png  binary
*.webp binary
*.jpg  binary
*.jpeg binary
*.pdf  binary
*.docx binary
*.xlsx binary
